$DB = '(DESCRIPTION =
      (ADDRESS = (PROTOCOL = TCP)(HOST = localhost)(PORT = 1521))
      (CONNECT_DATA =
      (SERVER = DEDICATED)
      (SERVICE_NAME = database_service_name)
      ))';

  $conn = oci_connect('your_username','your_dbpassword',$DB);
